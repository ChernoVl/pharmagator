spring:
  datasource:
    url: jdbc:postgresql:pharmagator
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate

pharmagator:
  data-providers:
    apteka-ants:
      url: https://anc.ua/productbrowser/v2/ua
      category-fetch-url: /categories